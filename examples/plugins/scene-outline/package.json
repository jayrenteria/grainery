{
  "name": "grainery-scene-manager",
  "private": true,
  "version": "1.3.1",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test test/*.test.mjs",
    "validate": "node ../../../scripts/grainery-plugin.mjs validate . --check-entry",
    "pack": "npm run build && node ../../../scripts/grainery-plugin.mjs pack ."
  },
  "devDependencies": {
    "@grainery/plugin-sdk": "file:../../../packages/plugin-sdk",
    "typescript": "~5.8.3"
  }
}
